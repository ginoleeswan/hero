// The two faction accents for the team clash — the app's canonical competitive
// pair (orange side A vs blue side B), matching the Arena hub blooms, the VS
// badge and the share-card vote split. Gold (goldAccent) stays the arbiter for
// the meter seam and winner crown, so identity never fights gameplay.
import { COLORS } from '../../constants/colors';

export const FACTION_A = COLORS.orange; // #E77333 (left side)
export const FACTION_B = COLORS.blue; // #15A1AB (right side)
