// app/franchise/[slug].tsx — franchise (sub-group within a universe) browse page.
// Renders the same source-aware browse screen as /category and /universe; the
// screen detects the /franchise route and matches heroes.franchise exactly.
export { default } from '../category/[slug]';
