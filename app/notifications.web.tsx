// expo-router resolves by platform extension and throws if a route has only one
// half of the pair. The inbox is a list either way, so this is the same screen.
export { default } from './notifications';
