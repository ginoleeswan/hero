// app/universe/[slug].tsx — universe (publisher/studio/franchise) browse page.
// Renders the same source-aware browse screen as /category (the screen resolves
// its own slug to a universe), so the two share one design with no duplication.
export { default } from '../category/[slug]';
