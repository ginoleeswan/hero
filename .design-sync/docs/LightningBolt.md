---
category: Decor
---

A jagged vertical lightning bolt (gold fill, dark outline) rendered as an SVG path — the divider between two hero panels in a matchup. Set `height` to the panel height; `width` defaults narrow (52) so portraits keep maximum space.

```tsx
<LightningBolt height={280} />
```
