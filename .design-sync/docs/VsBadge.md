---
category: Badges
---

Restrained typographic "VS" marker on a small disc — the editorial divider between two combatants. `glass` is a dark translucent disc for overlaying portraits; `solid` is an opaque navy medallion that reads on a light surface. Size via `size` (diameter in px).

```tsx
<VsBadge variant="solid" size={46} />
```
