---
category: Layout
---

A centred "or" divider — two hairlines flanking a small label — used between an action group and an alternative (e.g. between primary buttons and social sign-in). Override the word via `label`.

```tsx
<SocialDivider label="or" />
```
