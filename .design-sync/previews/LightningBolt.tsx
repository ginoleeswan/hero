import { LightningBolt } from 'mythique-ds';

export const Bolt = () => (
  <div style={{ padding: 24, background: '#293C43', display: 'flex', justifyContent: 'center' }}>
    <LightningBolt height={220} />
  </div>
);
